package com.agn.dioClass;

// Enumeration of operation types applicable to two datasets (arrays, collections etc.)
public enum EnumOperationType {
    LEFT_UNION,
    RIGHT_UNION,
    INNER_UNION,
    OUTER_UNION,
    DISTINCT_UNION
}
